[Nope] Hardcode charset

[Nope] Charset configurable

[ ] Render document in Template:
  -> wherever the site file is, look for a template.html
  -> if we find one, we read it in
  -> for every document, we compile to html, and put that html in the template

[ ] Literate Reason/OCaml:
  -> look for a template.re
  -> compile the template
  -> for every document
    -> compile to html
    -> execute the template on the document
    -> write the output to a file

[ ] Your Docs Are A Program

