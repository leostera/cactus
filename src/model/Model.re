type site = {
  name: string,
  path: string,
  dir: string,
  charset: string,
};

type project = {
  root: string,
  name: string,
  output_dir: string,
};

let project_filename = "cactus-project";

let site_filename = "site";
